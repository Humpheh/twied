* Folder contains files for building the Polygon database.
* These files are messy and unmaintained as they were only used once.
* Will require changes to update database location + shapefile location

offsetadder.py
    - Load the TZ offsets from the tzcodes csv file to the DB

gadmadder.py
    - Load GADM shapefile into the DB

tmshape.py
    - Load the TM world borders into the DB

tzshape.py
    - Load the TZ shapefile into the DB